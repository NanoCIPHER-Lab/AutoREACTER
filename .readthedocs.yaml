version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py # Update this path if your conf.py is located elsewhere

python:
  install:
    - requirements: docs/requirements.txt # Optional: if you have dependencies for autodoc